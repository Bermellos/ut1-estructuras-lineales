8)a) Es la opción c. Agrega un nodo al principio de la lista. this.primero era el primero de la lista y pasó a ser i, osea que la respuesta es la c porque agrega un elemento al inicio de la lista cada vez que se llama a la función.  

public void myFunction(int i) { 
Nodo nodo = new Nodo(i, this.primero); 
cabeza = nodo; 
} 
a. Agregar un nodo a continuación del nodo “cabeza”. 
b. Quitar el nodo de la lista. 
c. Agregar un nodo al principio de la lista. 
d. Agregar un nodo al final de la lista. 


8)b) Es la opción d. El nodo temporal a continuación del actual, ya que como la lista es circular no tiene fin. Siempre vuelve al inicio en algún momento u otro, por lo que se ejecuta indefinidamente.

actual = this.primero; 
while (actual.siguiente != null) 
actual = actual.siguiente; 
actual.siguiente = otroNodo; 
otroNodo.siguiente = null; 
a. Puede arrojar un resultado correcto si la lista fuera doblemente enlazada. 
b. Debe necesariamente usar un nodo especial de cabecera para que el resultado 
sea correcto. 
c. Puede dar un error en tiempo de ejecución si actual termina siendo null al 
terminar el bucle. 
d. Aunque actual no sea null al inicio, ejecuta indefinidamente. 


8)c) Es la opción d. El nodo temporal a continuación del actual, ya que con la primera instrucción, temporal ahora apunta al siguiente de actual y con la segunda instrucción, actual apunta a temporal. Por lo que termina quedando en medio temporal (actual ---> temporal ---> siguiente).

temporal.siguiente = actual.siguiente; 
actual.siguiente = temporal; 
a. Elimina de la lista al nodo actual. 
b. Elimina de la lista al nodo temporal. 
c. Inserta el nodo actual a continuación del nodo temporal. 
d. Inserta el nodo temporal a continuación del nodo actual. 


8)d) Es la opción a. Elimina de la lista al nodo temporal, ya que en la primera línea temporal apunta al mismo lugar que actual (a siguiente) y en la segunda actual.siguiente apunta al mismo lugar que temporal.siguiente (que es el mismo al que apunta siguiente, porque en la primera instrucción temporal apunta al mismo lugar que actual.siguiente), entonces siguiente queda borrado de la lista porque actual (la caebza de la lista) lo apunta a el. 
actual ---> siguiente ---> x 
temporal ---> siguiente

actual ---> x 
temporal ---> siguiente   

temporal = actual.siguiente;
actual.siguiente = temporal.siguiente; 
a. Elimina de la lista al nodo temporal. 
b. Elimina de la lista al nodo actual. 
c. Inserta el nodo actual a continuación del nodo temporal. 
d. Inserta el nodo temporal a continuación del nodo actual. 